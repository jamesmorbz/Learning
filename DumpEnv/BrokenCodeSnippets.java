package DumpEnv;
Dictionary Dictionary = new Hashtable();

System.out.println(unsortedDict);
Set<Set<String>> keys = unsortedDict.keySet();
System.out.println(keys);
for (Enumeration i = (unsortedDict).elements(); i.hasMoreElements();)
{
    System.out.println("Value in Dictionary : " + i.nextElement());
}

Dictionary Dictionary = new Hashtable();

int index = keys.indexOf(element);
for (int i = 0; i < keys.size(); i++) {
    System.out.println(unsortedDict.toString());
}
for (int i = 0; i < stringOfSports.length; i++) {
    dictList.add(unsortedDict.get(i).toString());
}


for (int i = 0; i < stringOfSports.length; i++) {
    Dictionary.get
}

        // FROM SAE8two.java
       //logic
        //boolean found = false;
        //int i = 0;
        //while(!found) {
        //    int checkValue = checkForCalculation(expression, i);
        //    if (checkValue ==2){
        //        found = true;
        //    }
        //    else{
        //        i++;
        //    }
        //
        //}

